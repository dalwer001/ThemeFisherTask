const videoButton = document.getElementById("videoButton");

videoButton.addEventListener('click',()=>{
    const videoButtonArea = document.getElementById("videoArea");
    videoButtonArea.style.display = "none";
})